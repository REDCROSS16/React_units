export default () => {

    const redColor = 'red';

    const styles = {
        fontWeight: 'bold', 
        color: 'orange',
        textTransform: 'uppercase'
    }

    return  (
        <div style={styles}>Hello</div>
    );
}